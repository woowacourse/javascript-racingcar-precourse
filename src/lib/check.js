export const GO_AHEAD_MAX_VALUE = 9;
export const MAX_CAR_NAME_LENGTH = 5;

const checkSpace = inputs => /\s/

export const checkValidCarNamesAndGetErrorMessage = inputs => {
  //공백이 들어간 경우 (함수로 분리)

  //자동차 숫자 길이가 5를 넘어가는 경우(함수로 분리)

  //중복되는 경우
}

export const checkValidTurnNumber = inputs => {
  //공백이 들어가는 경우 (위에서 쓴 함수 재탕)

  //숫자 외의 글자가 입력되는 경우
}