const { readCarName, readTryCount } = require('../view/InputView');

class Controller {
  readCarName() {
    readCarName();
  }

  readTryCount() {
    readTryCount();
  }
}

module.exports = Controller;
