import { $, $$ } from '../utils/element-tools.js';

export const $nameInput = $('#car-names-input');
export const $nameSubmit = $('#car-names-submit');

export const $countInput = $('#racing-count-input');
export const $countSubmit = $('#racing-count-submit');

export const $$countGroups = $$('*[data-group="count"]');
export const $$resultGroups = $$('*[data-group="result"]');
